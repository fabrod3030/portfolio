import React from 'react'
import { NavLink } from "react-router-dom"

function NavBar() {
    return (
        <header>
            <div>
                <nav>
                    <NavLink>
                        
                    </NavLink>
                </nav>
            </div>
        </header>
    )
}

export default NavBar
